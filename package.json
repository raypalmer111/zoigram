{
  "name": "zoigram",
  "version": "0.15.0",
  "private": true,
  "description": "Zoigram: photo sharing for real players inside the inZOI phone",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "build:locales": "node tools/build-locales.cjs",
    "server:test": "node --test server/test/*.test.cjs",
    "server:start": "node server/src/main.cjs",
    "package:manual": "node tools/package-nexus.cjs"
  },
  "engines": {
    "node": ">=20"
  }
}
